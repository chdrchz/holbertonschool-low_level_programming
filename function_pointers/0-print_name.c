#include ""
/**
 * print_name - ?
 *
 * Return: void
 */
void print_name(char *name, void (*f)(char *))
{
	f(char);
}

void (*f)(char *)
{	
	int counter;

	if (counter = 0; counter < name; counter++)
	{
		_putchar(char);
	}
}
