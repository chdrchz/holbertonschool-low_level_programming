#include "main.h"
#include <stdio.h>
/**
 * *_memset - ?
 *
 * Return: ?
 */
char *_memset(char *s, char b, unsigned int n)
{
	int i;
	char mem[n];

	if (s == NULL || n == 0)
	{
		return NULL;
	}

	for (i = 0; i < mem[n]; i++)
	{
		i++;
	}
}
