#include <stdio.h>
/**
 * main - using putchar function
 *
 * Return: 0
 */
int main(void)
{
	char x// x is my placeholder for every letter in the alphabet

	for(int x = 'a'; x <= 'z'; x++)
		putchar(x);
		putchar('\n');
	return(0);
}
